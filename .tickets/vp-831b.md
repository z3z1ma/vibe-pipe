---
"id": "vp-831b"
"status": "closed"
"deps": []
"links": []
"created": "2026-02-02T05:46:53Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"tags":
- "sprint:Codebase-Cleanup-and-De-bloating"
- "fanout"
"external": {}
---
# Sprint prep: Codebase Cleanup and De-bloating

Objective:
Clean up the codebase, remove dead code, outdated examples, and so on. Make every bit of bytes left in the codebase relevant and net useful.

## Sprint Brief

Objective restatement: Remove dead or misleading artifacts (conceptual examples, broken doc links) so the repo only contains runnable examples and accurate docs.

Sprint focus (2-5 words): Prune dead artifacts

Why this sprint focus is the best next step: Recent commits already removed outdated examples, but link rot and conceptual-only files still mislead users. Cleaning these now improves trust and reduces future cleanup churn.

Current state:
- Existing tickets that matter: Only `vp-831b` is open; no other sprint tickets exist yet.
- Codebase state that matters:
  - `git status` shows only untracked `.venv` locally (no repo changes).
  - Recent commits removed outdated examples and added the examples catalog.
  - Found cleanup targets: `examples/api_ingestion/pipeline_v2.py` is conceptual and references missing docs; `README.md` links to non-existent `docs/source/*.rst`; `docs/config_driven_pipelines.md` and `docs/source/config_driven_pipelines.md` reference missing `examples/pipelines/demo_pipeline.*` files.

Risks + unknowns (and how we'll resolve them):
- The conceptual `pipeline_v2` file may still be needed as design context. Resolve by searching for references; if needed, move to a docs/notes location instead of deleting.
- README link targets could be ambiguous between `docs/` and `docs/source/`. Resolve by linking to files that actually exist and are best for GitHub readers.
- Config-driven pipelines docs need a concrete example. Resolve by keeping an inline example or pointing to an existing, schema-matching file.

## Ticket Set

Created sprint tickets:
- `vp-8eec` - Remove conceptual API ingestion v2 example
- `vp-3945` - Fix README documentation link rot
- `vp-f2be` - Clean up config-driven pipelines doc example references

## Output

Created/updated ticket IDs: [vp-8eec, vp-3945, vp-f2be]

Suggested ordering + what can run in parallel:
- All three tickets are independent and can run in parallel.
- If ordering is preferred: do `vp-8eec` first, then `vp-3945` and `vp-f2be` in any order.

Sprint name: Codebase Cleanup and De-bloating
Sprint tag: sprint:Codebase-Cleanup-and-De-bloating

## Notes

**2026-02-02T06:01:05Z**

Sprint brief completed and ticket set created: vp-8eec, vp-3945, vp-f2be. All tickets independent; can run in parallel.
