---
"id": "vp-3945"
"status": "in_progress"
"deps": []
"links": []
"created": "2026-02-02T06:00:15Z"
"type": "task"
"priority": 2
"assignee": "z3z1ma"
"parent": "vp-831b"
"tags":
- "sprint:Codebase-Cleanup-and-De-bloating"
- "docs"
- "cleanup"
"external": {}
---
# Fix README documentation link rot

# Fix README documentation link rot

Objective alignment:
Ensure the primary README only points to existing documentation so newcomers don’t hit dead links.

Scope:
- Update broken documentation links in `README.md` (Core Topics list and API reference)
- Replace references to non-existent `docs/source/*.rst` files with existing docs paths

Non-goals:
- Create new docs or reorganize the docs hierarchy
- Rewrite documentation content beyond correcting links

Plan:
1. Identify broken links in `README.md` (e.g., `docs/source/api_reference.rst`, `docs/source/connectors.rst`).
2. Map each link to the closest existing doc (prefer `docs/*.md`; use `docs/source/api/index.rst` for API reference if needed).
3. Update `README.md` links and re-check for any `docs/source/*.rst` link rot.

Verification:
- `uv run pytest tests/docs/test_cli.py -q`

Acceptance criteria:
- `README.md` contains no links to non-existent doc files.
- Core Topics list resolves to actual files in the repo.

Risks/edge cases:
- If both `docs/` and `docs/source/` contain similar content, choose the path intended for GitHub readers.

Dependencies:
- None


## Acceptance Criteria

README.md no longer references non-existent docs; core docs links point to existing files.

## Notes

**2026-02-02T06:07:54Z**

Completed link fixes:

**Changes made:**
- docs/source/api_reference.rst → docs/source/api/index.rst (API Reference)
- docs/source/connectors.rst → docs/database_connectors.md (Database Connectors)
- docs/source/data_quality.rst → docs/quality-scoring.md (Data Quality)
- Removed non-existent links: error_handling.rst, integration_guide.rst
- Contributing.rst kept (file exists)

**Verification:**
- All linked files verified to exist
- uv run pytest tests/docs/test_cli.py -q: 6 passed
- Git commit: ed6ac7e

**Risks:** None - all replacement docs exist and are relevant to their topics.
