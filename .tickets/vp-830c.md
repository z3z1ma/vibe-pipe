---
"id": "vp-830c"
"status": "in_progress"
"deps":
- "vp-8866"
"links": []
"created": "2026-01-31T18:19:10Z"
"type": "task"
"priority": 2
"assignee": "z3z1ma"
"tags":
- "sprint:Testing-Tooling-Cohesion"
- "testing"
- "docs"
"external": {}
---
# Update testing docs to align with uv + integration markers

Context:
- docs/testing_patterns.md uses bare pytest commands and doesn't mention uv-run or integration marker behavior.
- AGENTS.md mandates uv for running tests.

Scope:
- Update docs/testing_patterns.md to use uv run pytest commands consistently.
- Add integration test guidance: pytest -m integration, pytest -m "not integration", and scripts/run_integration_tests.sh.
- Note that *_integration.py files are treated as integration tests once vp-8866 lands.

Dependencies:
- vp-8866 (integration marker behavior must be defined before documenting it).

Suggested order:
- After vp-8866.

Parallelization:
- Can run in parallel with helper export cohesion ticket.

## Acceptance Criteria

- All command examples in docs/testing_patterns.md use uv run pytest (or uv run ...)
- Integration test instructions match repo behavior (markers + script)
- Doc changes stay scoped to testing_patterns.md (unless a second doc is explicitly needed)

## Notes

**2026-01-31T19:40:46Z**

Updated docs/testing_patterns.md to align with uv and integration markers:

Changes made:
- All pytest command examples now use 'uv run pytest' prefix
- Added integration test section with marker usage:
  - uv run pytest -m integration (run only integration tests)
  - uv run pytest -m "not integration" (run only unit tests)
  - Documented scripts/run_integration_tests.sh script
- Added note about *_integration.py auto-marking as integration tests
- Added note about test infrastructure requirements
- Added snapshot testing command reference (uv run pytest --update-snapshots)
- Added 'Run by Keyword' example (uv run pytest -k)

All changes scoped to docs/testing_patterns.md as required.
Verified scripts/run_integration_tests.sh exists.

Commit: 3119523
